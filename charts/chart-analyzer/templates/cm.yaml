apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chart-analyzer.fullname" . }}-cm
  labels:
    {{- include "chart-analyzer.labels" . | nindent 4 }} 
data:
  {{- range $key, $value := .Values.configEnv }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}


