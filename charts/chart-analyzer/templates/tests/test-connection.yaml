apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-analyzer.fullname" . }}-test-connection"
  labels:
    {{- include "chart-analyzer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-analyzer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
