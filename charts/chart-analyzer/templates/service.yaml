apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-analyzer.fullname" . }}
  labels:
    {{- include "chart-analyzer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-analyzer.selectorLabels" . | nindent 4 }}
